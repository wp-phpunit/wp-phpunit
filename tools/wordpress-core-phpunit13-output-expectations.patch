diff --git a/tests/phpunit/tests/general/template.php b/tests/phpunit/tests/general/template.php
index f8702b91a5..cce0616680 100644
--- a/tests/phpunit/tests/general/template.php
+++ b/tests/phpunit/tests/general/template.php
@@ -131,12 +131,14 @@ class Tests_General_Template extends WP_UnitTestCase {
 	#[\PHPUnit\Framework\Attributes\Group('site_icon')]
 	#[\PHPUnit\Framework\Attributes\RequiresFunction('imagejpeg')]
 	public function test_site_icon_url() {
-		$this->expectOutputString( '' );
+		ob_start();
 		site_icon_url();
+		$this->assertSame( '', ob_get_clean() );
 
 		$this->set_site_icon();
-		$this->expectOutputString( $this->site_icon_url );
+		ob_start();
 		site_icon_url();
+		$this->assertSame( $this->site_icon_url, ob_get_clean() );
 	}
 
 	/**
@@ -187,8 +189,9 @@ class Tests_General_Template extends WP_UnitTestCase {
 	#[\PHPUnit\Framework\Attributes\Group('site_icon')]
 	#[\PHPUnit\Framework\Attributes\RequiresFunction('imagejpeg')]
 	public function test_wp_site_icon() {
-		$this->expectOutputString( '' );
+		ob_start();
 		wp_site_icon();
+		$this->assertSame( '', ob_get_clean() );
 
 		$this->set_site_icon();
 		$output = array(
@@ -200,8 +203,9 @@ class Tests_General_Template extends WP_UnitTestCase {
 		);
 		$output = implode( "\n", $output );
 
-		$this->expectOutputString( $output );
+		ob_start();
 		wp_site_icon();
+		$this->assertSame( $output, ob_get_clean() );
 	}
 
 	/**
@@ -210,8 +214,9 @@ class Tests_General_Template extends WP_UnitTestCase {
 	#[\PHPUnit\Framework\Attributes\Group('site_icon')]
 	#[\PHPUnit\Framework\Attributes\RequiresFunction('imagejpeg')]
 	public function test_wp_site_icon_with_filter() {
-		$this->expectOutputString( '' );
+		ob_start();
 		wp_site_icon();
+		$this->assertSame( '', ob_get_clean() );
 
 		$this->set_site_icon();
 		$output = array(
@@ -224,9 +229,10 @@ class Tests_General_Template extends WP_UnitTestCase {
 		);
 		$output = implode( "\n", $output );
 
-		$this->expectOutputString( $output );
 		add_filter( 'site_icon_meta_tags', array( $this, 'custom_site_icon_meta_tag' ) );
+		ob_start();
 		wp_site_icon();
+		$this->assertSame( $output, ob_get_clean() );
 		remove_filter( 'site_icon_meta_tags', array( $this, 'custom_site_icon_meta_tag' ) );
 	}
 
@@ -431,8 +437,9 @@ class Tests_General_Template extends WP_UnitTestCase {
 	 */
 	#[\PHPUnit\Framework\Attributes\Group('custom_logo')]
 	public function test_the_custom_logo() {
-		$this->expectOutputString( '' );
+		ob_start();
 		the_custom_logo();
+		$this->assertSame( '', ob_get_clean() );
 
 		$this->set_custom_logo();
 
@@ -449,8 +456,9 @@ class Tests_General_Template extends WP_UnitTestCase {
 
 		$image = wp_get_attachment_image( $this->custom_logo_id, 'full', false, $custom_logo_attr );
 
-		$this->expectOutputString( '<a href="http://' . WP_TESTS_DOMAIN . '/" class="custom-logo-link" rel="home">' . $image . '</a>' );
+		ob_start();
 		the_custom_logo();
+		$this->assertSame( '<a href="http://' . WP_TESTS_DOMAIN . '/" class="custom-logo-link" rel="home">' . $image . '</a>', ob_get_clean() );
 	}
 
 	/**
diff --git a/tests/phpunit/tests/post/Tests_Post_Thumbnail_Template.php b/tests/phpunit/tests/post/Tests_Post_Thumbnail_Template.php
index 62895ddfd6..c1470e1ad4 100644
--- a/tests/phpunit/tests/post/Tests_Post_Thumbnail_Template.php
+++ b/tests/phpunit/tests/post/Tests_Post_Thumbnail_Template.php
@@ -213,13 +213,15 @@ class Tests_Post_Thumbnail_Template extends WP_UnitTestCase {
 
 	public function test_the_post_thumbnail() {
 
-		$this->expectOutputString( '' );
+		ob_start();
 		the_post_thumbnail();
+		$this->assertSame( '', ob_get_clean() );
 
 		$GLOBALS['post'] = self::$post;
 
-		$this->expectOutputString( '' );
+		ob_start();
 		the_post_thumbnail();
+		$this->assertSame( '', ob_get_clean() );
 
 		set_post_thumbnail( self::$post, self::$attachment_id );
 
@@ -232,8 +234,9 @@ class Tests_Post_Thumbnail_Template extends WP_UnitTestCase {
 			)
 		);
 
-		$this->expectOutputString( $expected );
+		ob_start();
 		the_post_thumbnail();
+		$this->assertSame( $expected, ob_get_clean() );
 	}
 
 	/**
@@ -274,13 +277,15 @@ class Tests_Post_Thumbnail_Template extends WP_UnitTestCase {
 	public function test_the_post_thumbnail_url() {
 		$GLOBALS['post'] = self::$post;
 
-		$this->expectOutputString( '' );
+		ob_start();
 		the_post_thumbnail_url();
+		$this->assertSame( '', ob_get_clean() );
 
 		set_post_thumbnail( self::$post, self::$attachment_id );
 
-		$this->expectOutputString( wp_get_attachment_url( self::$attachment_id ) );
+		ob_start();
 		the_post_thumbnail_url();
+		$this->assertSame( wp_get_attachment_url( self::$attachment_id ), ob_get_clean() );
 	}
 
 	/**
